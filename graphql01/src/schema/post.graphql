type Post {
  id: Int
  title: String
  content: String
  description: String
  user: User
}

type Query {
  posts: [Post]
  post(id: Int!): Post
}

type Mutation {
  createPost(
    title: String!
    content: String!
    description: String
    id_lead: Int!
  ): Post
}
